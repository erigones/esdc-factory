---
pkgin_mirror: "https://pkgsrc.danube.cloud"
pkgin_release: 2019Q4
pkgin_arch: x86_64

pkgin_repositories:
  -        "{{ pkgin_mirror }}/packages/SmartOS/{{ pkgin_release }}/{{ pkgin_arch }}/All"
  - "https://pkgsrc.joyent.com/packages/SmartOS/{{ pkgin_release }}/{{ pkgin_arch }}/All"

pkgin_repository_keys:
  - "{{ pkgin_mirror }}/GPG.key.pub"
